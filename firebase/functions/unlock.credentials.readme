encrypt with
openssl enc -aes-256-cbc -salt -in credentials.js -out credentials.enc

decrypt with
openssl enc -aes-256-cbc -d -in credentials.enc -out credentials.js

passphrase tip:
my usual algo for frontier